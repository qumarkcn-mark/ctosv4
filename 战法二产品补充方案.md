# 战法二产品补充方案

> 版本：v1.0 | 日期：2026-04-23
> 本文件是对《雷达重设计方案 v2.0》的增量补充。
> 现有 v2.0 覆盖了战法一（三级别共振买入），本文补充战法二（中枢上沿突破）以及两套战法共用的持仓判断逻辑。

---

## 一、现状与缺口

《雷达重设计方案 v2.0》的入场五条件（第2.1节）本质上是**战法一**的结构化表达：

```
① 日线二买或类二买结构
② 30分二买，与日线共振
③ 5分底背驰（入场门控）
④ 入场窗口有效（距底 < 10%）
⑤ 周线无压制
```

这套条件无法识别**战法二（中枢上沿突破）**，因为：
- 战法二的日线状态是"中枢内"，不是"二买"
- 战法二的关键信号是"小级别在中枢上沿积蓄后突破"，入场条件完全不同

---

## 二、产品需要支持双战法扫描

### 2.1 扫描结果分类（取代现有的A/B/C三情景）

```
现有：雷达输出 甲/乙/丙 三种情景，地位平权

修改后：系统先判断战法类型，再在对应战法内输出状态

┌─────────────────────────────────────────────┐
│  扫描结果                                    │
│                                             │
│  ✅ 战法一：三级别共振      [触发价/止损价]   │
│  ✅ 战法二：中枢上沿突破    [触发价/止损价]   │
│  ⏳ 都不符合               [继续观察]        │
└─────────────────────────────────────────────┘
```

### 2.2 战法二的入场条件检测

新增 `_detect_strategy_2()` 方法：

| # | 条件 | 检测方法 |
|---|------|----------|
| ① | 日线存在明确中枢 | `_detect_zhongshu(day)` 且震荡次数 ≥ 3 |
| ② | 当前价格在日线中枢上沿附近 | `abs(current_price - zhongshu.zg) / zhongshu.zg < 0.05` |
| ③ | 小级别（30分/5分）在中枢上沿形成震荡 | 30分中枢存在，中枢中心在日线ZG ± 3% 范围内 |
| ④ | 5分钟出现第三类买点 | 5分中枢向上突破，回踩不破中枢上沿 |
| ⑤ | 周线无明显压制 | 同战法一 |

**补充判断：震荡收敛度（可选）**

```python
# 判断小级别震荡是否在收窄（能量积累的信号）
m30_oscillation_narrowing = (
    recent_high_range < previous_high_range * 0.85
)
# 如果是收窄型，战法二置信度更高
```

---

## 三、两套战法的统一止损逻辑

两套战法的止损来源相同，无需区分：

```
统一止损：5分钟介入中枢的 ZG（中枢上沿）
触发条件：current_price < m5_entry_zhongshu.zg
触发动作：立即推送通知（不是静默显示）
```

现有 v2.0 第2.6节的强制出局规则（③当前价 ≤ 止损价）已覆盖此逻辑，无需修改。

---

## 四、新增：持仓判断规则（"预期未兑现"逻辑）

这是本次最重要的新增逻辑，补充到 v2.0 第2.4节"预案验证规则"之后。

### 4.1 两种"没有按预期走"的情况

开仓后，系统需要区分以下两种状态：

**状态A：结构还在，价格在磨**
```
判断：current_price > m5_entry_zhongshu.zg （价格在5分中枢上方）
含义：止损未触发，结构有效，市场只是慢
行动：持有，不推送干扰通知
禁止：因为"时间过长"主动出场
```

**状态B：结构已失效**
```
判断：current_price ≤ m5_entry_zhongshu.zg （价格跌回5分中枢内）
含义：5分钟3买的结构假设被证伪
行动：立即推送"结构失效"通知，建议出场
推送文案："{股票} 5分中枢支撑失效，入场逻辑已证伪，建议出场"
```

### 4.2 补充到现有 Stage 0（走势验证期）

在现有 Stage 0 的监控字段中，新增：

```python
"stage_0_extended": {
    "m5_zhongshu_intact": bool,        # 5分入场中枢是否完整（价格是否还在中枢上方）
    "structure_status": str,            # "结构完整" / "结构失效"
    "holding_rationale": str,           # "等待走出 | 结构支撑中" / "结构已失效，建议出场"
}
```

### 4.3 AI叙事约束补充

在现有 `RADAR_SYSTEM_PROMPT` 中新增约束：

```
当 structure_status = "结构失效" 时：
  你只能说"5分入场中枢已被跌破，入场假设失效"
  不能说"等待反弹"、"关注支撑"或任何暗示继续持有的话

当 m5_zhongshu_intact = true 且价格没有大涨时：
  你可以说"结构仍然有效，价格在消化，继续持有"
  不能说"市场可能不会按预期走"或制造不必要的焦虑
```

---

## 五、持仓模式与空仓模式的彻底隔离

### 5.1 核心原则

持仓期间价格每天变动，结构每天演化。这带来一个产品设计的关键决策：

**持仓分析 ≠ 入场分析的每日重复。**

```
入场分析回答：要不要进？（一次性判断，入场后锁定）
持仓分析回答：要不要出？（每天更新，只看出场信号）
```

两套问题，两套信息架构，不能混用。

### 5.2 持仓期间每天更新的内容

系统每天只做三件事，按优先级执行：

```
第一优先：检查原始止损
  current_price < 入场时5分中枢ZG → 立刻推送"止损触发"，不再看其他

第二优先：更新台阶止损
  新的30分钟中枢ZG形成 且 高于现有台阶止损 → 台阶上移
  台阶只上移，永不下调

第三优先：检查背驰信号
  30分钟顶背驰确认 → Stage 4，推送减仓建议
  日线顶背驰确认   → Stage 5，推送清仓建议
```

这三件事之外，持仓期间不做其他分析。

### 5.3 持仓期间不允许重新入场分析

以下是高频错误场景，产品设计需要主动防止：

```
Day 1：入场，战法一结构成立
Day 3：价格横盘，用户重新看图
       → 系统不应再展示"入场机会评分"
       → 不应展示"战法条件checklist"
       → 不应给出新的A/B/C情景
       这些信息会让用户开始怀疑原始判断，产生不必要的出场冲动
```

**规则：持仓模式下，隐藏所有入场相关分析。**

### 5.4 UI 隔离规范

| 信息类型 | 空仓模式 | 持仓模式 |
|----------|----------|----------|
| 入场条件checklist | ✅ 展示 | ❌ 隐藏 |
| 战法识别（要不要进） | ✅ 展示 | ❌ 隐藏 |
| ATR止损合理性 | ✅ 展示 | ❌ 隐藏 |
| 入场机会评分 | ✅ 展示 | ❌ 隐藏 |
| 台阶止损 / 锁定利润 | ❌ 隐藏 | ✅ 展示 |
| 持仓阶段（Stage 0-5） | ❌ 隐藏 | ✅ 展示 |
| 背驰信号监控 | ❌ 隐藏 | ✅ 展示 |
| 浮盈/浮亏 | ❌ 隐藏 | ✅ 展示 |

### 5.5 "价格在磨"状态的处理

持仓期间常见场景：价格没有大涨，也没有触发止损，用户感到焦虑。

系统应明确展示：

```
持仓状态：结构完整，等待走出
  原始止损：15.00（入场中枢ZG，未触发）
  台阶止损：15.00（尚未上移）
  当前结论：价格在消化，结构有效，持有
```

**不允许**在此状态下展示任何暗示"市场可能不走"的内容。
**不允许** AI 在此状态下提出"可以考虑观望"之类的话。

---

## 七、UI 新增：战法标签

在雷达主面板顶栏增加战法标签：

```
┌─────────────────────────────────────────────┐
│ 600519  贵州茅台    [战法二：中枢突破 ●]      │
└─────────────────────────────────────────────┘
```

颜色规范：
- 战法一标签：`#10B981`（绿色系，趋势延续）
- 战法二标签：`#F59E0B`（黄色系，启动型，信号更少见）
- 都不符合：`#888888`（灰色，观察中）

---

## 八、任务分解（增量）

以下任务在 v2.0 七个Phase的基础上新增，建议插入 Phase 1 执行：

| 优先级 | 任务 | 文件 | 说明 |
|--------|------|------|------|
| P0 | 实现 `_detect_strategy_2()` | `chan_service.py` | 战法二入场条件检测 |
| P0 | 扫描结果改为战法分类输出 | `chan_service.py` | 取代现有A/B/C平权输出 |
| P0 | 5分中枢完整性监控 | `price_monitor.py` | 结构失效实时检测 + 推送 |
| P0 | 持仓/空仓模式彻底隔离 | `TRadarV2.jsx` | 持仓模式下隐藏所有入场分析字段 |
| P0 | 台阶止损每日更新逻辑 | `price_monitor.py` | 每天收盘后检测新30分中枢，自动上移台阶止损 |
| P1 | Stage 0 新增 `structure_status` 字段 | `chan_service.py` | 补充持仓判断依据 |
| P1 | "结构完整，等待走出"状态展示 | `TRadarV2.jsx` | 横盘时主动显示"结构有效，持有"，防止用户焦虑 |
| P1 | AI叙事约束补充 | `radar_prompt.py` | 结构失效禁止安慰；结构有效禁止制造焦虑 |
| P2 | UI战法标签 | `TRadarV2.jsx` | 顶栏显示当前识别的战法类型 |

---

## 九、不变的部分

以下 v2.0 内容无需修改，两套战法共用：

- 第2.2节：止损规则（ATR合理性校验）
- 第2.3节：仓位规则
- 第2.4节：预案验证规则（Stage 0逻辑）
- 第2.5节：持仓管理六阶段状态机
- 第2.6节：强制出局规则
- 第三节：三层系统架构
- 第五节至第八节：UI、API、测试规范

---

*本文档版本：v1.1*
*依赖文档：雷达重设计方案 v2.0、两套战法交易手册 v1.0*
