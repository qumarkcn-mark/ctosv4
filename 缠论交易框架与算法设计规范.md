# 缠论交易框架与算法设计规范

> 本文档整理自实盘交易逻辑讨论，作为 CT-OS V4 chan_service.py 算法重构的系统性参考。
> 算法的每一个模块都应能对应到本文档中的某一条规则，而不是孤立的补丁。

---

## 一、核心认知：算法与人脑的本质差距

人工推演之所以比算法更准确，原因有三：

**1. 动势感知 vs 静态检测**
人脑看到的是"正在发生什么"——价格从哪里来、力度如何、节奏是否加速。
算法只知道"当前状态是什么"——价格在中枢之上还是之下，节点类型是什么。
算法检测不到"价格已经从买点反弹了15%，入场窗口已经关闭"。

**2. 多级别同时融合 vs 串行叠加**
人脑同时权衡周线/日线/30分/5分，四个信号在一个判断里融合。
算法串行计算，日线→30分→5分逐层输出，合并时信息有损失，上下级别的共振关系没有被检测。

**3. 叙事完整性**
人工推演同时给出：当前位置、入场条件、止损位、目标位、时机是否已过。
算法目前只给出：节点类型 + 止损位，没有目标价，没有时机判断，没有级别共振评分。

---

## 二、多级别操作框架（用户标准流程）

### 2.1 各级别职责定义

| 级别 | 职责 | 回答的问题 |
|------|------|------------|
| **周线** | 宏观风险扫描 | 大方向是否安全？是否存在顶背驰或周线下跌中枢压制？ |
| **日线** | 寻找主操作买点 | 是否出现二买或类二买？这是想参与的那笔行情的结构起点 |
| **30分** | 买点共振确认 + 止损锚定 | 30分中枢在日线方向是否有充分行程？30分是否也出现二买/类二买？止损放哪？ |
| **5分** | 执行触发 | 是否出现三买或二买？距底部还有多远？现在进是否还在合理区间？ |

### 2.2 完整入场决策流程

```
Step 1：周线扫描
  - 周线是否处于上涨趋势中？（中枢之上 / 走出上涨线段）
  - 是否有周线级别顶背驰风险？
  - 若周线有明显压制 → 降低仓位预期，只做反弹而非趋势

Step 2：日线定位买点
  - 日线是否出现二买结构？
    · 条件：前有一买，反弹形成中枢，回调不破中枢ZG，有底背驰配合
  - 或日线类二买？
    · 条件：下跌线段结束，次级反弹后再次下探但不破前低，底背驰配合
  - 记录日线买点价格区间（ZG附近 ± ATR）

Step 3：30分共振验证
  - 30分中枢是否在日线方向有足够行程？（至少走出两段以上）
  - 30分是否同样出现二买或类二买结构？
  - 两个级别的买点价格区域是否重叠？
  → 重叠 = 高置信度底部，两个级别的参与者都在同一价位认为是低点

Step 4：确定止损位
  - 优先选 30分一买低点（结构性止损，该位置若破则30分结构整体无效）
  - 备选 日线一买低点（若30分结构已确立且较近）
  - 止损是结构性的，不是ATR倍数的机械计算

Step 5：5分执行入场
  - 在日线+30分买点区域内，等5分出现三买或二买信号
  - 三买：5分已形成中枢并向上突破（说明短线空头已被消化）
  - 二买：5分回调不破5分中枢ZG，底背驰配合
  - 关键判断：5分执行点距底部（30分一买低点）的距离
    · 距离 < 5%：理想入场，止损空间充裕
    · 距离 5-10%：可接受，适当减仓
    · 距离 > 10%：窗口偏离，不追，等次级回踩
```

### 2.3 预案验证规则（入场后第一监控）

入场是基于对走势的预判，预判必须在有限时间内得到验证，否则主动出场。

**逻辑依据：**
5分三买入场，背后的完整逻辑链是：
```
日线二买 → 日线要走出上涨
30分二买 → 30分要走出至少一笔向上   ← 最近一环，最先验证
5分三买  → 5分中枢突破，是30分上涨笔的启动信号
```
如果入场后30分没有走出上涨笔，逻辑链第二环断裂，预案失效，主动出场。
这不是"等止损"，是"预案本身没有实现"，属于主动判断，通常比止损更早、亏损更小。

**验证窗口：** 入场后至多10根30分K线（约2个交易日）

**三种结果：**

| 结果 | 条件 | 操作 |
|------|------|------|
| 验证通过 | 入场后30分形成完整上涨笔（出现顶分型，≥5根K线） | 进入持仓Stage 1，按持仓管理规则继续 |
| 预案失效 | 入场后30分走出向下笔（出现新低） | 主动出场，5分三买为假突破 |
| 时间失效 | 超过10根30分K线，未见完整上涨笔 | 主动出场，市场节奏不符预期 |

**与止损的关系：**
两者同时监控，互不替代，哪个先触发出哪个。
预案验证失效通常比止损更早，是第一道防线。

### 2.4 止损与目标

| 项目 | 规则 |
|------|------|
| **出场第一道** | 30分预案验证失效（入场后30分走出向下笔 或 超时未见上涨笔） |
| **止损位** | 30分一买低点（首选）或30分中枢ZG |
| **止损性质** | 结构性止损，破了说明中枢结构无效，不是ATR机械倍数 |
| **目标位** | 30分前高 / 日线中枢顶 / 日线线段终点（取最近的） |
| **减仓位** | 30分出现一卖或顶背驰信号时，先减一半 |
| **清仓位** | 日线级别出现顶背驰或三卖结构 |

---

## 三、缠论基础规则（算法实现的地基）

### 3.1 背驰的精确定义

文档中所有"底背驰"/"顶背驰"必须对应以下三种之一，不能混用：

| 背驰类型 | 适用场景 | 量化方法 |
|----------|----------|----------|
| **段背驰** | 识别一买/一卖 | 最后一段下跌（上涨）的MACD柱面积 < 前一段同向的面积 |
| **笔背驰** | 识别二买/二卖 | 最后一笔（回调）的MACD柱面积 < 前一笔同向的面积 |
| **盘整背驰** | 中枢内部短线机会 | 中枢内最后一笔极值比前一笔更低/更高，但MACD面积更小 |

**算法实现要求：**
- `_calc_beichi(bis, macd)` 需要区分以上三种类型并分别计算
- 一买的背驰判断用"段"级别（相邻两个下跌线段的MACD面积比较）
- 二买的背驰判断用"笔"级别（中枢下方最后一笔的MACD面积比较）
- 不能用同一个函数对所有买点做同样的背驰判断

### 3.2 买点的级别归属规则

买点的级别由**形成该买点的中枢级别**决定：

```
30分中枢之上的回调低点 → 30分二买（30分级别买点）
日线中枢之上的回调低点 → 日线二买（日线级别买点）
5分中枢被向上突破后的回踩 → 5分三买（5分级别买点）
```

**算法实现要求：**
- `_detect_lifecycle_node` 的返回值必须包含 `node_level` 字段
- 级别归属决定止损位的级别：30分买点→止损30分结构，日线买点→止损日线结构
- 不同级别的买点不能共用同一个止损逻辑

### 3.3 走势类型对买点有效性的影响

缠论的走势分类直接影响哪种买点更可靠：

| 走势类型 | 定义 | 有效买点类型 |
|----------|------|-------------|
| **盘整** | 只有一个中枢，价格在中枢范围内震荡 | 三买（等突破确认方向后才入场） |
| **趋势** | 多个同向中枢依次推进（每个中枢比上一个更高/更低） | 二买（回踩中枢顶是主要买点） |
| **转折** | 趋势末端出现段背驰，一买/类一买出现 | 一买/类一买（高风险，需配合大级别确认） |

**算法实现要求：**
- `_classify_zoushi` 的输出必须明确区分以上三种类型
- 盘整中的"三买"必须标注"等突破确认，警惕假突破"
- 趋势中的"二买"才是高置信度入场信号

### 3.4 三买的精确定义（当前文档有误）

> ❌ 错误理解：价格 > ZG = 三买区域出现

> ✅ 正确定义：价格突破ZG **之后**，第一次回调**不跌破ZG**，这个回调低点才是三买

三买完整识别流程：
```
1. 确认中枢已形成（至少三笔重叠）
2. 价格向上突破中枢ZG（突破笔收盘 > ZG）
3. 出现回调（至少一笔向下）
4. 回调低点 > ZG（没有跌回中枢）
5. 回调出现底背驰（笔级别即可）→ 三买确认
```

注意：突破当天不是三买，必须等回调后确认不破ZG才是三买入场位。

### 3.5 中枢的有效性标准

算法识别的中枢必须满足：
- 至少三笔重叠（一上一下一上，或一下一上一下）
- ZG = 三笔重叠区间的最低高点
- ZD = 三笔重叠区间的最高低点
- GG（最高高点）和 DD（最低低点）作为中枢延伸边界

**中枢扩张**：当价格回到中枢内后再次走出，中枢的GG/DD边界扩展，但ZG/ZD不变。
算法中`_build_zs_context`必须处理中枢扩张情形，不能把扩张后的GG当成ZG。

### 3.6 中枢内震荡状态的操作规则

当 `dep = inside`（价格在中枢内部）时：

```
状态：中枢震荡
操作逻辑：
  - 不是建仓时机，等待突破方向确认
  - 若出现盘整背驰（中枢内部的高点/低点创新极值但MACD面积缩小）
    → 短线参考意义，不作为主力仓位入场信号
  - 等价格突破ZG（向上）或破ZD（向下）后再判断方向
输出建议：
  - "中枢震荡，等方向确认后操作"
  - 附注当前中枢ZG和ZD值，让用户知道突破方向
```

---

## 四、买卖点完整体系

### 4.1 买点体系（三级）

| 买点 | 结构条件 | 背驰类型 | 置信度 | 止损位 |
|------|----------|----------|--------|--------|
| **一买** | 下跌线段终点，段背驰确认 | 段背驰 | 中（结构最差，风险最高） | 一买低点下方1ATR |
| **类一买** | 类似一买但线段确认不完整 | 段背驰或盘整背驰 | 低 | 同上，仓位减半 |
| **二买** | 一买后形成中枢，突破后回调不破ZG | 笔背驰 | 高 | 中枢ZG |
| **类二买** | 中枢确认不完整的二买结构 | 笔背驰 | 中 | 类二买低点 |
| **三买** | 中枢ZG突破后回调不破ZG | 笔背驰（可选） | 高（趋势确认后） | 中枢ZG |
| **类三买** | 突破后回调刚好在ZG附近，背驰弱 | 弱笔背驰 | 中 | ZG下方0.5ATR |

**关键规则：**
- 类买卖点的算法输出置信度必须比标准买卖点低一档
- 类一买在没有大级别方向确认时，不作为主力仓位入场信号

### 4.2 卖点体系（三级，与买点完全对称）

| 卖点 | 结构条件 | 背驰类型 | 操作建议 |
|------|----------|----------|----------|
| **一卖** | 上涨线段终点，段顶背驰确认 | 段顶背驰 | 清仓，风险最高信号 |
| **类一卖** | 类似一卖但线段确认不完整 | 段背驰或盘整背驰 | 减仓50%，剩余等确认 |
| **二卖** | 一卖后形成中枢，跌破后反弹不过ZD | 笔顶背驰 | 减仓至轻仓或清仓 |
| **类二卖** | 中枢确认不完整的二卖结构 | 笔顶背驰 | 减仓30-50% |
| **三卖** | 中枢ZD跌破后反弹不过ZD | 笔顶背驰（可选） | 清仓，趋势已转向 |
| **类三卖** | 跌破后反弹在ZD附近，背驰弱 | 弱笔顶背驰 | 减仓，警惕 |

**持仓管理触发规则：**
```
出现30分一卖/类一卖 → 减仓50%，止损上移到入场成本
出现30分二卖/三卖  → 清仓
出现日线一卖信号   → 无论持仓多少，全部清仓
```

### 4.3 5分底背驰作为入场门控（P1，非P3）

> ⚠️ 原文档将"5分底背驰作为门控"列为P3，这是错误的。

**正确定位：5分底背驰是入场的必要条件，不是加分项。**

```
无5分底背驰 = 价格到了位置，但没有信号 → 不入场
有5分底背驰 = 价格到了位置，且有确认信号 → 可以入场
```

算法实现：`forward_classes` 的 `condition` 字段必须包含5分背驰状态：
```json
{
  "condition": "等5分笔背驰确认（当前5分背驰尚未形成）",
  "entry_gate": {
    "m5_beichi": false,
    "m5_beichi_note": "5分MACD尚未出现背驰，信号未触发"
  }
}
```

---

## 五、二买与类二买的识别规则

### 3.1 标准二买

**结构条件（缺一不可）：**
1. 前有一买（下跌线段结束，底背驰确认）
2. 反弹形成一个中枢（至少三段交叉）
3. 向上离开中枢（三买区域出现，价格 > ZG）
4. 回调但不破中枢ZG
5. 回调低点出现底背驰（MACD或笔级别背驰）

**入场窗口判断：**
- 窗口开启：底背驰形成后
- 窗口有效：价格从底背驰低点反弹幅度 < 前一上涨段的38%
- 窗口关闭：反弹幅度 > 38%，或价格已超越前一段高点50%以上

### 3.2 类二买

**结构条件：**
1. 下跌线段结束
2. 次级别出现反弹，但力度不足以确立新中枢（或中枢刚刚成立）
3. 再次下探但不破前低（高于一买低点）
4. 下探出现底背驰

**特点：**
- 级别确认不完整，胜率略低于标准二买
- 大级别趋势向上时，类二买是最早的低吸机会
- 止损更紧：放在类二买低点下方（前低下方1个ATR）

### 5.3 算法识别要点

当前算法 `_detect_lifecycle_node` 只判断"结构是否存在"，需要补充：

```
检测项 1：入场窗口是否仍有效
  - 取最近一个二买/类二买的低点价格 (entry_low)
  - 计算 distance = (current_price - entry_low) / entry_low
  - distance < 0.05 → 窗口有效
  - distance 0.05-0.10 → 窗口收窄，备注"建议减仓入场"
  - distance > 0.10 → 窗口关闭，输出"等次级回踩"

检测项 2：5分底背驰是否已确认（必要条件，非可选）
  - 需要5分MACD在买点低位出现笔背驰
  - 没有5分背驰 = 价格到了但信号没到，不能入场，输出"信号未触发"

检测项 3：是否破了止损参考位
  - current_price < 30分一买低点 → 结构无效，清空买点输出

检测项 4：窗口关闭后的处理（原文档缺失）
  - 窗口关闭 ≠ 机会消失
  - 等待"次级回踩"：价格从高点回调，不破二买低点，在二买低点上方再次出现5分底背驰
  - 次级回踩失效条件：回调破了二买低点 → 原结构失效，不再等次级
```

---

## 四、级别共振检测（当前算法缺失）

### 4.1 共振的定义

当日线和30分在**相近价格区域**同时出现二买/类二买结构时，称为"级别共振"。
共振意味着多个时间框架的参与者都在同一价位认为是低点，胜率显著高于单级别买点。

### 4.2 共振评分规则（建议实现）

```python
def _calc_resonance_score(day_state, m30_state, current_price):
    score = 0
    reasons = []

    # 日线有买点结构
    if day_state.get("node") in ["二买", "类二买", "一买"]:
        score += 40
        reasons.append(f"日线{day_state['node']}结构")

    # 30分有买点结构
    if m30_state.get("node") in ["二买", "类二买", "一买"]:
        score += 30
        reasons.append(f"30分{m30_state['node']}结构")

    # 两个级别买点价格区域重叠
    day_zg = day_state.get("zs_operative_zg", 0)
    m30_zg = m30_state.get("zs_operative_zg", 0)
    if day_zg > 0 and m30_zg > 0:
        overlap = abs(day_zg - m30_zg) / day_zg
        if overlap < 0.03:  # 价格区域相差3%以内
            score += 20
            reasons.append("日线/30分买点价格共振")

    # 周线方向确认（无周线压制）
    # TODO: 接入周线数据后补充

    return {"score": score, "max": 90, "reasons": reasons}
```

### 4.3 共振输出格式

```json
{
  "resonance": {
    "score": 70,
    "max": 90,
    "grade": "高",
    "reasons": ["日线类二买结构", "30分二买结构", "日线/30分买点价格共振"],
    "summary": "日线与30分买点共振，底部置信度高"
  }
}
```

---

## 五、止损位计算规范（重构）

### 5.1 当前问题

现在的止损计算混用了两种逻辑：
- ATR倍数机械止损（在 atr_service.py 中）
- 中枢ZD/ZG结构止损（在 chan_service.py 中）

两者在输出时有时冲突，AI拿到后不知道用哪个。

### 5.2 统一规范

止损位必须是**结构性止损**，按以下优先级取：

```
优先级 1：30分操作中枢 ZG（当前价在中枢之上时）
  - 意义：这是价格刚刚突破的支撑，破了说明30分突破失效

优先级 2：30分一买低点
  - 意义：下跌结构的终点，破了说明整个反弹结构无效

优先级 3：日线一买低点
  - 意义：最后防线，破了说明日线趋势未确立

ATR仅作参考：
  - 如果结构性止损距当前价 > 2.5 × ATR，提示"止损过宽，谨慎入场"
  - 如果结构性止损距当前价 < 0.5 × ATR，提示"止损过窄，可能是假突破"
```

### 5.3 目标价计算（当前缺失，需新增）

```python
def _calc_target_price(level_state, current_price):
    targets = []

    # 目标1：操作中枢顶（ZG）
    zg = level_state.get("zs_operative_zg")
    if zg and current_price < zg:
        targets.append({"price": zg, "label": "操作中枢顶", "priority": 1})

    # 目标2：线段高点（前高）
    seg_high = level_state.get("seg_high")
    if seg_high and seg_high > current_price:
        targets.append({"price": seg_high, "label": "线段前高", "priority": 2})

    # 目标3：上一级别中枢顶
    upper_zg = level_state.get("upper_zg")
    if upper_zg and upper_zg > current_price:
        targets.append({"price": upper_zg, "label": "上级别中枢顶", "priority": 3})

    return sorted(targets, key=lambda x: x["price"])
```

---

## 六、Forward Classes 输出规范（重构）

### 6.1 当前字段（不完整）

```json
{
  "label": "甲：二买入场",
  "condition": "等30分底背驰确认",
  "stop_loss": 14.83,
  "confidence": "高"
}
```

### 6.2 目标字段（完整版）

```json
{
  "label": "甲：二买入场",
  "node_type": "二买",
  "node_level": "30分",
  "condition": "等5分底背驰确认，30分ZG=15.82为支撑",
  "entry_zone": {"low": 15.60, "high": 16.00},
  "stop_loss": 14.83,
  "stop_loss_basis": "30分一买低点",
  "stop_loss_atr_check": "止损距当前价1.8×ATR，合理",
  "targets": [
    {"price": 17.92, "label": "30分前高", "priority": 1},
    {"price": 19.50, "label": "日线中枢顶", "priority": 2}
  ],
  "window_status": "有效",
  "window_distance_pct": 3.2,
  "resonance_score": 70,
  "resonance_grade": "高",
  "timing_note": "5分执行点距底部3.2%，止损空间充裕，可入场",
  "confidence": "高"
}
```

---

## 七、算法改造优先级

### P0（影响核心判断正确性，需立即修复）

| 问题 | 位置 | 修复方案 |
|------|------|----------|
| 操作中枢选择错误 | `_build_zs_context` | 已修复：price>ZG取最高ZG的中枢 |
| 持仓路径死代码 | `_build_holding_classes` | 已修复：analyze_matrix_state 传入 holding |
| 持仓数据流断裂 | agent.py / chan.py | 已修复：先查持仓再调用分析 |

### P1（影响输出质量，本轮重点）

| 问题 | 位置 | 修复方案 |
|------|------|----------|
| 入场窗口时机检测 | `_detect_lifecycle_node` | 增加 distance_from_entry 计算 |
| 无目标价字段 | `_build_forward_analysis` | 新增 `_calc_target_price` |
| 级别共振未检测 | `analyze_matrix_state` | 新增 `_calc_resonance_score` |

### P2（提升AI叙事质量）

| 问题 | 位置 | 修复方案 |
|------|------|----------|
| RADAR prompt 缺少距底部信息 | RADAR_SYSTEM_PROMPT | 在 context_json 加入 window_distance_pct |
| 止损叙事和结构来源不对应 | forward_classes | 加入 stop_loss_basis 字段 |
| `_classify_zoushi` 叙事旧路径 | chan_service.py | 对齐到新中枢选择逻辑 |

### P1（补充，原P3升级）

| 问题 | 位置 | 修复方案 |
|------|------|----------|
| 5分底背驰未作为入场门控 | `_detect_lifecycle_node` + forward_classes | 入场必要条件，加入 `entry_gate.m5_beichi` 字段 |
| 背驰类型未区分（段/笔/盘整） | `_calc_beichi` | 重构背驰计算，按买点类型选择对应背驰类型 |
| 买点未标注级别归属 | `_detect_lifecycle_node` | 加入 `node_level` 字段（哪个级别的中枢形成的买点） |
| 走势类型对买点置信度的影响 | `_classify_zoushi` + forward_classes | 盘整中三买降置信度，趋势中二买提置信度 |
| 三买定义有误（价格>ZG≠三买） | `_detect_lifecycle_node` | 修正：必须是突破后回调不破ZG才算三买 |

### P2（影响输出质量）

| 问题 | 位置 | 修复方案 |
|------|------|----------|
| 入场窗口时机检测 | `_detect_lifecycle_node` | 增加 distance_from_entry 计算 |
| 无目标价字段 | `_build_forward_analysis` | 新增 `_calc_target_price` |
| 级别共振未检测 | `analyze_matrix_state` | 新增 `_calc_resonance_score` |
| 卖点体系缺失 | 持仓路径 | 实现一卖/二卖/三卖识别，触发减仓/清仓提醒 |
| 中枢内震荡状态无操作指引 | `_detect_lifecycle_node` | dep=inside 时输出方向等待建议 |

### P3（提升AI叙事质量）

| 问题 | 位置 | 修复方案 |
|------|------|----------|
| RADAR prompt 缺少距底部信息 | RADAR_SYSTEM_PROMPT | 在 context_json 加入 window_distance_pct |
| 止损叙事和结构来源不对应 | forward_classes | 加入 stop_loss_basis 字段 |
| `_classify_zoushi` 叙事旧路径 | chan_service.py | 对齐到新中枢选择逻辑 |

### P4（完整性补充，下一阶段）

| 问题 | 位置 | 修复方案 |
|------|------|----------|
| 无周线数据接入 | chan_service.py | 接入周线缠论计算，补充宏观风险扫描 |
| 窗口关闭后次级回踩检测 | `_detect_lifecycle_node` | 增加次级回踩有效性判断 |
| 中枢扩张处理 | `_build_zs_context` | GG/DD扩张时不能更新ZG/ZD |

---

## 八、数据字段补充需求

`_analyze_single_level` 返回字典需新增以下字段，供上层函数使用：

```python
{
    # 现有字段（略）...

    # 新增：入场时机相关
    "entry_low": float,           # 最近二买/类二买低点价格
    "distance_from_entry": float, # 当前价距entry_low的百分比
    "window_status": str,         # "有效" / "收窄" / "已过"

    # 新增：目标价
    "targets": list,              # [{price, label, priority}]

    # 新增：共振（在 analyze_matrix_state 层计算）
    "resonance": dict,            # {score, grade, reasons, summary}

    # 新增：止损来源说明
    "stop_loss_basis": str,       # "30分一买低点" / "30分中枢ZG" / "日线一买低点"
    "stop_loss_atr_check": str,   # ATR合理性备注

    # 新增：线段高低点（用于目标价计算）
    "seg_high": float,            # 最近上涨线段高点
    "seg_low": float,             # 最近下跌线段低点
}
```

---

## 十、设计原则

1. **每个字段有明确来源**：止损来自哪条规则、节点来自哪级别结构，必须可追溯
2. **算法不补丁，规则先定义**：改任何逻辑前，先确认它对应本文档哪条规则
3. **AI是叙事层，不是判断层**：所有判断（节点/止损/时机/共振）由算法完成，AI只负责把结构化数据翻译成自然语言
4. **级别职责不混用**：周线给方向，日线找买点，30分共振确认，5分触发执行
5. **止损必须结构性**：ATR只做合理性校验，不能作为止损的唯一依据
6. **背驰必须分类**：段背驰/笔背驰/盘整背驰不能混用，对应不同买卖点类型
7. **买点必须有门控**：5分背驰是入场必要条件，不是可选项
8. **类买卖点必须降级**：类买卖点在置信度/仓位建议上必须比标准买卖点低一档
9. **走势类型决定买点有效性**：盘整中的三买和趋势中的二买胜率不同，算法必须区分输出

---

---

## 十一、关键问题速查表

> 遇到算法输出不合理时，先对照这张表找根因。

| 症状 | 根因 | 对应规范章节 |
|------|------|-------------|
| 说"等二买"但价格已经涨了很多 | 入场窗口未检测 | 五节 5.3 检测项1 |
| 二买/三买置信度相同但逻辑不同 | 走势类型未区分 | 三节 3.3 |
| 中枢内震荡无操作建议 | dep=inside 未处理 | 三节 3.6 |
| 止损位是ATR倍数而不是结构低点 | 止损规范未执行 | 六节 5.2 |
| "底背驰"但MACD没有明显背驰 | 背驰类型混用 | 三节 3.1 |
| 三买信号在价格突破当天出现 | 三买定义有误 | 三节 3.4 |
| 持仓股只有止损，没有减仓信号 | 卖点体系未实现 | 四节 4.2 |
| 入场后30分反向但未到止损，不知道该怎么办 | 预案验证规则缺失 | 二节 2.3 |
| 止损还没到但感觉走势不对 | 预案验证规则缺失 | 二节 2.3 |
| 日线买点和30分买点无关联 | 级别共振未检测 | 原五节（级别共振） |
| 类二买和二买输出完全相同 | 类买点未降级 | 四节 4.1 |

*最后更新：2026-04-21*
*对应代码：server/services/chan_service.py*
