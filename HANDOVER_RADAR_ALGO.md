# 交接文件：TRadar 推演算法重设计
> 会话时间：2026-04-19  
> 项目：CT-OS V4.0 交易教练

---

## 一、已完成的工作

### 1. 兆易创新 显示修复
`server/services/rotation_scorer.py` 已修改：
- `_empty_score` 返回 score=20（原来是0），触发"全仓清仓"建议
- `_action_from_score` score<30 时持仓返回"全仓清仓"（原来是"清仓"）
- `score_symbol` 把 `is_holding` 传入 `_empty_score`

---

## 二、调仓罗盘重设计结论

### 核心结论
**去掉评分系统，改成推演预案展示。**

用户不需要系统替他判断（综合分/建议文案）。
需要的是：客观缠论结构 + 条件触发预案，自己横向对比后决策。

### 目标展示形式
每个标的展开后显示：
- 当前结构（走势类型、级别位置、买卖点状态）
- 🟢 预案A：触发条件 → 推演 → 具体仓位指令（含股数）
- 🔴 预案B：触发条件 → 推演 → 具体仓位指令
- 🔴 预案C：触发条件 → 推演 → 具体仓位指令

### 现有系统缺口
- `forward_classes` 甲/乙/丙已生成，但罗盘只取第一条前60字
- 触发价位未结构化提取
- 仓位指令未跟持仓数量动态绑定
- 多预案展开UI未实现

---

## 三、TRadarV2 推演算法问题（核心议题）

### 已确认的三个缺陷

**缺陷一：不知道是否持仓**
```python
# 现在
def _build_forward_classes(l1, l2, l3)
# 应该
def _build_forward_classes(l1, l2, l3, holding=None)
# holding = None（空仓）或 {"qty": 2000, "cost": 125}
```

**缺陷二：级别混用（最重要）**
价格在 l2（30分）ZG 上方时，丙情形用的是30分ZG作为触发，
但30分ZG可能距当前价25%，A股当日根本触不到。

正确逻辑：
```
丙情形（当日可触发）→ 用 l3（5分）ZG
丁情形（多日才触发）→ 用 l2（30分）ZG
```

**缺陷三：空仓时无买卖点生命周期判断**
现在对空仓者输出"加仓/减仓/离场"预案，毫无意义。
空仓者需要：当前处于买卖点哪个阶段 + 转化条件 + 等待区间。

### 止损呈现方式（已确认设计原则）
止损价不单独跳出来作为规则，而是**融入结构叙述**：
- ❌ "止损303.18，跌破清仓"
- ✅ "5分ZG=303.18被跌破，次级别推升结束，30分顶背驰得到确认。持仓已大幅盈利，结构判断不利时继续持有缺乏依据。"

成本信息改变的是**语气和重心**，不是结论：
- 浮盈143%：结构信号不利时，持有缺乏依据
- 微亏：结构偏弱时，持仓机会成本上升
- 空仓：不在结构破坏时介入

---

## 四、新算法设计（已确认方向）

### 主入口改造
```python
def _build_forward_classes(l1, l2, l3, holding=None):
    if holding is None:
        return _build_empty_position_classes(l1, l2, l3)
    else:
        return _build_holding_classes(l1, l2, l3, holding)
```

### 空仓路径：买卖点生命周期状态机

生命周期节点：
```
卖点侧：一卖_触发 / 二卖_触发 / 三卖_触发 / 中枢内顶背驰
买点侧：一买_机会 / 二买_形成 / 三买_确认 / 中枢内底背驰
过渡态：中枢_震荡 / 构建中
```

转化链条（核心逻辑）：
```python
TRANSFORMATION_MAP = {
  "一卖_触发":    {"next": "一买_机会",    "watch": "下跌段出现底背驰"},
  "三卖_触发":    {"next": "一买_机会",    "watch": "下跌加速后底背驰（三卖转一买）"},
  "中枢内顶背驰": {"next": "中枢内底背驰", "watch": "中枢下沿附近出现底背驰"},
  "一买_机会":    {"next": "二买_形成",    "watch": "反弹后回踩不创新低"},
  "二买_形成":    {"next": "三买_确认",    "watch": "突破ZG后回踩守住"},
}
```

多级别置信度：
```
5分底背驰                    → 轻仓试探
30分底背驰                   → 标准仓位
5分+30分区间套               → 高置信度
日线+30分+5分三级区间套      → 极高置信度，重仓
```

### 持仓路径：结构威胁 + 盈亏融合叙述

止损层级（技术止损和成本保护取高）：
```python
pnl_pct = (price - cost) / cost
if pnl_pct >= 1.0:   cost_floor = cost * 1.5
elif pnl_pct >= 0.5: cost_floor = cost * 1.2
elif pnl_pct >= 0.2: cost_floor = cost * 1.0
else:                cost_floor = cost * 0.95

effective_stop = max(technical_stop, cost_floor)
# 但不作为跳出的规则，融入叙述
```

预案级别修正（关键）：
```
丙情形改用 l3（5分）ZG 作为当日触发
丁情形用 l2（30分）ZG 作为结构红线（多日）
```

---

## 五、实战分析案例（算法验证）

### 688048 长光华芯（价格328.01）

| 级别 | 走势 | 距ZG | 背驰 |
|---|---|---|---|
| 60分 | 复杂盘整↑5↓1 | +50% | 0/100（扩张） |
| 30分 | 复杂盘整↑5↓1 | +34% | **80/100 ⚠️顶背驰** |
| 5分 | 复杂盘整 | +8.2% | 50/100 |

- 30分ZG=244（当日根本触不到，-25%）
- 5分ZG=303.18（当日可触发，-7.6%）→ 真正的丙情形应在这里

**持仓2000股成本135（浮盈143%）操作：**
- 现在减仓1000股锁定利润
- 剩余1000股止损5分ZG=303.18（结构叙述融入）
- 5分顶分型出现再减500股
- 跌破303.18清剩余

**空仓视角：** 30分一卖刚触发，不介入。等回调至303~294（5分底背驰）或244~260（30分底背驰）。

---

### 300724（价格110.23）

| 级别 | 阶段 | 核心信号 |
|---|---|---|
| 60分 | **一买机会** | 底背驰100/100，ZD下方-8.6% |
| 30分 | 突破ZG等回踩 | ZG=108.12已破，顶背驰50/100 |
| 日线 | 中枢内7%位置 | ZD=108.00支撑 |

**空仓策略：** 等回踩108~109守住（30分ZG）后入场，不追当前价。

**持仓2000股成本125（浮亏11.8%）：**
- 不是卖点（60分底背驰真实）
- 唯一防线：日线ZD=108.00（30分ZG=108.12）
- 守住108：持仓等修复
- 跌破108：止损，不重复"再等等"的错误

---

## 六、待实现的代码改动

优先级顺序：
1. `_build_forward_classes` 拆分为空仓/持仓两条路径
2. 丙情形从 l2_zg 改为 l3_zg（当日可触发）
3. 空仓路径实现买卖点生命周期状态机
4. 持仓路径的成本融入叙述生成
5. 调仓罗盘UI展示多预案（展开形式）

---

## 七、设计原则（已达成共识）

1. **系统不替用户判断**，给客观结构 + 条件，用户自己决策
2. **止损融入叙述**，不作为单独跳出的规则
3. **持仓/空仓分路**，两种状态对同一张图得出完全不同的分析
4. **级别时间匹配**：当日预案用5分级别触发，多日预案用30分/日线
5. **盈亏上下文融入语气**：浮盈越多，离场叙述越轻松；亏损状态，叙述聚焦结构依据
6. **买卖点转化**：空仓分析必须识别当前处于哪个买卖点生命周期节点

