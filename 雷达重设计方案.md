# 雷达重设计方案 v2.0

> 基于完整交易规则体系的雷达重构方案。
> 核心目标：损失可控 + 胜率过半 = 长期盈利。
> 本文档同时覆盖后端算法、API接口、前端UI三个层面。

---

## 一、设计原则

**算法是唯一的判断层，AI是唯一的叙事层。**

- 所有结构判断（节点/止损/时机/共振/阶段）由算法完成，输出结构化JSON
- AI只负责把算法结论翻译成自然语言，不允许自行发明判断
- 用户永远能看到算法的原始输出（结构状态面板），不依赖AI解读

**持仓状态决定信息优先级。**

- 空仓模式：回答"要不要进"
- 持仓模式：回答"怎么拿、怎么出"
- 两种模式信息架构完全不同，不能混用

---

## 二、交易规则体系（算法实现的完整依据）

### 2.1 入场规则（控制胜率）

以下五个条件必须**全部满足**才能输出"可入场"信号：

| # | 条件 | 检测方法 | 权重 |
|---|------|----------|------|
| ① | 日线出现二买或类二买结构 | `_detect_lifecycle_node(day)` | 必要 |
| ② | 30分出现二买或类二买，与日线价格区域共振 | `_calc_resonance_score` 价格偏差 < 3% | 必要 |
| ③ | 5分出现底背驰（入场门控） | `_calc_beichi(m5, type="笔")` | 必要 |
| ④ | 入场窗口有效（5分执行点距底部 < 10%） | `distance_from_entry < 0.10` | 必要 |
| ⑤ | 周线无明显压制 | 周线状态非"下跌趋势中段" | 必要 |

任何一个条件不满足，输出状态为"观察中"而非"可入场"，并说明缺失哪个条件。

### 2.2 止损规则（控制单次亏损）

**止损位计算优先级：**

```
优先级1：30分操作中枢 ZG（价格在中枢之上时）
优先级2：30分一买低点（中枢下方结构低点）
优先级3：日线一买低点（最后防线）
```

**ATR合理性校验（必须通过才能入场）：**

```
止损距离 = (current_price - stop_price) / current_price
合理范围：1×ATR ≤ 止损距离 ≤ 2.5×ATR

< 1×ATR  → 止损太紧，正常波动会被扫出 → 不入场，等回调
> 2.5×ATR → 止损太宽，亏损超标 → 不入场，等更好位置
```

### 2.3 仓位规则（控制总风险敞口）

```
每笔最大风险 = 账户总资金 × 风险比例（默认1%，用户可调）
建议仓位（股数）= 最大风险金额 ÷ (current_price - stop_price)
建议仓位（金额）= 建议股数 × current_price
```

算法需要输出建议仓位，用户自行决定是否采用。

### 2.4 预案验证规则（入场后第一监控，优先级高于止损）

入场后最先触发的监控机制。**预案失效比止损更早出现，亏损更小。**

**核心逻辑：**
5分三买入场，意味着预定了30分要走出一笔向上。如果30分没有走出来，逻辑链从第二环就断了，不是"等止损"，是"预案本身失效"，主动出场。

**验证窗口：** 入场后至多2个交易日（10个30分K线）

**三种结果：**

```
结果1 验证通过：
  入场后30分走出完整上涨笔（出现顶分型，至少5根30分K线）
  → 预案成立，进入持仓管理Stage 1

结果2 预案失效（主动出场）：
  入场后30分走出一笔向下（出现新低）
  → 说明30分结构未启动，5分三买是假突破，主动出场
  → 此时价格通常还未触及止损，亏损小于止损亏损

结果3 时间失效（主动出场）：
  入场后超过10根30分K线，仍未形成完整上涨笔
  → 市场没有按预期节奏运动，出场等待更清晰的信号
```

**与止损的关系：**
```
入场后同时监控两个条件：
  条件A（预案验证）：30分是否走出上涨笔   ← 优先，通常更早触发
  条件B（价格止损）：current_price < stop_price
哪个先触发出哪个，两者不互斥
```

**算法实现：新增 Stage 0**

```python
# Stage 0：走势验证期（入场后立即进入）
{
  "stage": 0,
  "label": "走势验证期",
  "monitoring": {
    "m30_bi_direction_since_entry": str,  # "向上" / "向下" / "未形成"
    "m30_bi_complete": bool,              # 是否已形成完整向上笔
    "bars_since_entry": int,              # 入场后经过的30分K线数
    "validation_deadline": int,           # 还剩几根30分K线到验证截止
  },
  "status": str,  # "验证中" / "验证通过" / "预案失效" / "时间失效"
  "action": str   # "继续持有" / "建议出场（预案失效）" / "建议出场（超时）"
}
```

### 2.5 持仓管理规则（保住利润）

**止盈状态机（完整六阶段，Stage 0为验证期）：**

| 阶段 | 触发条件 | 止损位置 | 操作建议 |
|------|----------|----------|----------|
| Stage 0 走势验证期 | 刚入场，30分未完成上涨笔 | 原始止损（30分ZG） | 等待30分走出上涨笔，超时或反向主动出场 |
| Stage 1 验证期 | 30分上涨笔已形成，浮盈 < 1×止损距离 | 原始止损（30分ZG） | 守住，验证判断 |
| Stage 2 保本期 | 浮盈 ≥ 1×止损距离 | 上移至成本线 ±0.5% | 止损上移，保住不亏 |
| Stage 3 利润保护期 | 浮盈 ≥ 2×止损距离 | 跟踪最近30分中枢ZG | 台阶追踪，让利润跑 |
| Stage 4 减速预警 | 出现30分顶背驰 | 台阶止损不变 | 减仓50%，剩余守台阶 |
| Stage 5 趋势终结 | 出现日线顶背驰 或 跌破台阶止损 | — | 清仓 |

**台阶止损更新逻辑：**

```python
# 每次价格突破新的30分中枢ZG，台阶止损上移
if current_price > new_center_zg:
    trailing_stop = new_center_zg  # 上移到新中枢ZG
    # 永远只上移，不下移
```

**背驰类型区分（中继 vs 转折）：**

```
30分顶背驰出现后：
  → 等待后续走势
  → 价格随后创新高 = 中继背驰，只上移止损，不减仓
  → 价格无法创新高开始横盘/回落 = 转折背驰，触发Stage 4减仓
```

### 2.6 强制出局规则（不给亏损扩大机会）

以下任何一条触发，**必须推送通知**，不是静默显示：

```
① 预案失效：入场后30分走出向下笔 → 主动出场（Stage 0触发）
② 时间失效：入场后10根30分K线未见上涨笔 → 主动出场（Stage 0触发）
③ 当前价 ≤ 止损价 → 立即出场
④ 当前价 < 30分一买低点 → 结构破坏，出场
⑤ 日线顶背驰确认 → 无论盈亏，清仓
```

出局优先级：①② > ③④ > ⑤
预案验证失效通常比止损更早触发，是第一道防线。

---

## 三、系统架构：三层模型

```
┌─────────────────────────────────────────┐
│  Layer 1：结构层（纯算法，实时计算）       │
│  输出：节点/中枢/止损/目标/共振/窗口状态   │
├─────────────────────────────────────────┤
│  Layer 2：推演层（规则引擎，生成甲乙丙）   │
│  输出：持仓阶段/入场评分/建议仓位/预案卡片  │
├─────────────────────────────────────────┤
│  Layer 3：AI叙事层（按需触发，LLM翻译）    │
│  输出：自然语言解读（有约束，不能发明判断） │
└─────────────────────────────────────────┘
```

---

## 四、两种操作模式

### 4.1 空仓模式（入场机会视角）

**自动触发条件：** positions 表中该股票持仓为0或不存在

**结构状态面板字段：**

```
入场机会评分：  X / 5（五个条件满足几个）
缺失条件：      [5分背驰未形成] [窗口已收窄]
操作节点：      30分二买
节点级别：      30分
共振状态：      日线/30分共振 ✓
入场区间：      15.60 - 16.00
止损参考：      14.83（30分中枢ZG）
止损ATR校验：   1.8×ATR，合理 ✓
目标价1：       17.92（30分前高）
目标价2：       19.50（日线中枢顶）
盈亏比：        约2.1:1
建议仓位：      基于1%风险，约XX股
入场窗口：      有效（距底3.2%）
```

**甲/乙/丙定义（空仓模式）：**

| 预案 | 含义 | 触发状态 |
|------|------|----------|
| 甲 可入场 | 五个条件全满足 | 绿色激活 |
| 乙 等待信号 | 结构存在，1-2个条件未满足 | 黄色等待，注明缺什么 |
| 丙 结构无效 | 关键条件（止损位被破）失效 | 红色，建议观望 |

### 4.2 持仓模式（仓位管理视角）

**自动触发条件：** positions 表中该股票持仓 > 0

**结构状态面板字段：**

```
持仓阶段：      Stage 3 利润保护期
成本价：        15.20
当前价：        18.60
浮动盈亏：      +22.4% / +3,400元
台阶止损：      17.20（最近30分中枢ZG）
距台阶止损：    -7.5%（还有7.5%空间）
锁定利润：      +13.2%（即使触发台阶止损也保住这部分）
30分背驰：      未出现
日线背驰：      未出现
目标价1：       19.50（30分前高，剩余距离+4.8%）
目标价2：       22.00（日线中枢顶，剩余距离+18.3%）
```

**甲/乙/丙定义（持仓模式）：**

| 预案 | 含义 | 触发状态 |
|------|------|----------|
| 甲 续涨持有 | 无背驰，台阶止损未触及，结构健康 | 绿色，给出继续持有依据 |
| 乙 减速减仓 | 30分顶背驰出现 或 Stage 4触发 | 黄色，建议减仓比例和价位 |
| 丙 出场信号 | 日线顶背驰 或 台阶止损触及 | 红色，清仓建议 + 推送通知 |

---

## 五、UI布局方案

### 5.1 整体结构与信息层级 (Information Architecture)

**核心原则：先给结论（操作建议），再给论据（数据面板）。**

如果用户只有 3 秒钟看盘，他们最需要知道的 3 件事：
1. 股票代码和状态（我是谁、我现在什么模式）
2. 我该做什么（甲/乙/丙预案哪一个是亮的）
3. 执行的边界条件（止损点/入场区在哪里）

因此，将行动卡片置于数据面板之上，形成“行动倒推”的阅读流：

```text
┌─────────────────────────────────────────────┐
│ [股票代码]  [股票名称]          [持仓模式 ●]  │  ← 顶栏，状态徽标 (第一眼级)
├─────────────────────────────────────────────┤
│  甲 [绿/黄/灰]  乙 [绿/黄/灰]  丙 [红/灰]   │  ← 三预案卡片 (第二眼级，核心行动)
│  点击可展开执行细节                           │
├─────────────────────────────────────────────┤
│                                             │
│         结构状态面板                          │  ← 数据论据层 (第三眼级，折叠或紧凑显示)
│         （空仓/持仓 字段不同）                │
│                                             │
├─────────────────────────────────────────────┤
│  [AI解读]  按需点击，有约束的自然语言叙事       │  ← 情绪安抚层 (默认折叠)
└─────────────────────────────────────────────┘
```

### 5.2 状态颜色规范

| 颜色 | 含义 |
|------|------|
| 绿色 | 条件满足 / 可操作 / 结构健康 |
| 黄色 | 部分满足 / 等待 / 注意 |
| 红色 | 出场信号 / 结构失效 / 止损触发 |
| 灰色 | 不适用 / 信息不足 |

### 5.3 推送通知规范

以下事件**必须**推送微信小程序订阅消息：

```
① 止损价触及（任何持仓股票）     → "⚠️ {股票} 止损价触及，请检查持仓"
② 30分顶背驰确认                → "📉 {股票} 30分顶背驰出现，建议减仓评估"
③ 日线顶背驰确认                → "🚨 {股票} 日线顶背驰，建议清仓"
④ 入场五条件全满足（关注股）      → "📈 {股票} 入场信号触发，建议关注"
```

### 5.4 交互状态覆盖 (Interaction States)

| 组件 / 场景 | Loading (加载中) | Empty (空状态/无数据) | Error (异常/降级) | Success (理想态) | Partial (部分数据) |
|-------------|------------------|-----------------------|-------------------|------------------|--------------------|
| **雷达主面板** | 柔和的骨架屏 (Skeleton)，保留顶栏和三大卡片轮廓，骨架屏使用微光呼吸动画 | 提示“尚未收录结构数据”，提供温和的文案（如：“该标的尚无级别结构，去看看自选股吧”）并带有一个主要的“返回”操作按钮 | 顶部出现醒目的黄色软警告条（Soft Warning）：“实时数据获取失败，当前展示为本地缓存数据（最后更新于 XX:XX）”，下方正常渲染旧数据，供用户做历史结构参考 | 完整渲染行动卡片和数据论据面板 | 数据面板某项缺失时（如缺乏日线数据），用灰色标注“数据不足”，但不阻塞主流程 |
| **行动卡片** | 卡片内指标展示转圈 Loading | 置灰，不可点击，文案为“暂无结论” | 卡片正常展示基于旧数据的结论，但在卡片右上角增加警告图标 | 绿/黄/红状态明确点亮，高对比度显示 | — |
| **AI 解读** | “AI 正在疯狂思考中...” 以及生成动画 | — | 提示“AI 服务暂时开小差了”，允许用户仅依靠纯算法面板操作 | 自然语言文本排版舒适，重点加粗 | — |

> **补充说明：数据新旧（Staleness）的判断标准**
> *   **交易时间内（09:30-11:30, 13:00-15:00）**：若最后更新时间距今超过 5 分钟（即缺失了最新的 5 分钟 K 线），则判定为“过期数据”，触发软警告。
> *   **非交易时间（盘后）**：若数据没有包含当天 15:00 的收盘数据，则判定为“过期数据”。周末及节假日则以最后一个交易日 15:00 为准。

### 5.5 用户旅程与情绪弧线 (User Journey)

| 阶段 | 用户行为 (User Does) | 情绪状态 (User Feels) | 设计支持 (Plan Specifies) |
|------|----------------------|-----------------------|---------------------------|
| **5秒内 (Visceral)** | 进入个股雷达页面 | 焦虑、急于寻找答案 | 第一眼看到大字号的绿/黄/红行动卡片，瞬间获得“行/不行/等”的明确结论。 |
| **5分钟 (Behavioral)** | 查看结构状态面板核对依据 | 谨慎、求证 | 清晰的表格或紧凑的数据面板（如：“距止损 -7.5%”），用数字打消疑虑。 |
| **盘中剧烈波动时** | 看到价格急跌，惊慌打开页面 | 恐惧、冲动 | AI解读面板平时折叠，点开后提供极度冷酷、客观的机器翻译语言（如：“当前价格跌破台阶止损，执行出局。”），用无情绪的指令切断用户的幻想。 |
| **5年长线 (Reflective)** | 长期复盘雷达的表现 | 信任、依赖 | 通过严格分离“客观算法”与“主观解读”，建立长期信任。即便止损，也是系统纪律的体现。 |

---

### 5.6 UI 具体视觉规范 (UI Tokens)

为了避免生成毫无重点的“通用 SaaS 面板”（AI Slop Risk），明确以下视觉规范，本页面归类为 **APP UI** 风格：

1. **字体系统 (Typography)**：禁止使用浏览器默认字体或系统无差别渲染。数字强制使用 `Inter` 或 `Roboto Mono` 保证对齐。中文字体使用系统原生无衬线体 (`-apple-system, BlinkMacSystemFont, "PingFang SC"`)。行动卡片标题使用 `font-weight: 700`，字号 `20px`。
2. **色彩令牌 (Color Tokens)**：不使用默认的纯红纯绿。
   * **Action-Green**: `#10B981` (背景色带 `10%` 透明度作为底色，文字和边框使用纯色)。
   * **Action-Red**: `#EF4444`。
   * **Action-Yellow**: `#F59E0B`。
   * **Surface-Dark**: 纯粹的暗色背景 `#121212`，面板背景使用 `#1E1E1E`，不需要多余的阴影 (Box-shadow)，使用 `1px solid #333` 边框进行区隔。
3. **结构隔离**：数据面板不是卡片网格的堆砌，而是类似 IDE 面板的属性列表 (Key-Value pairs)，使用等宽对齐，`label` 使用 `#888888`，`value` 使用 `#E5E5E5`。
4. **卡片控件风格**：三大行动卡片采用**扁平化指示灯 (Flat Indicator)** 风格。未激活状态仅保留低对比度边框；激活状态采用对应颜色的高对比度纯色填充，无渐变、无投影，确保视觉上极度克制。

### 5.7 移动端与无障碍规范 (Responsive & A11y)

1. **移动端适配 (< 768px)**：
   * **行动卡片**：从横向并排切换为**垂直堆叠 (Vertical Stack)**，每张卡片占据 100% 宽度，确保文字不折行且点击区域足够大。
   * **数据面板**：从表格形态切换为单列 Key-Value 列表，Label 靠左，Value 靠右对齐。
2. **无障碍 (A11y)**：
   * 确保所有的红/黄/绿状态不仅依靠颜色区分，必须带有明确的图标或文字标签（例如在红色状态旁增加 `[警示]` 标签），照顾红绿色弱用户。
   * 所有的可点击区域（行动卡片展开、AI 解读展开）最小触摸目标尺寸必须达到 `44px × 44px`。

### 5.8 边缘状态决策 (Edge Cases)

*   **目标价到达但未触发背驰**：如果价格达到了计算出的“目标价1”或“目标价2”，但此时结构上并没有出现 30 分顶背驰，系统**不改变主卡片状态，也不推送通知**。仅在数据面板中将该目标价项高亮（例如亮起绿色），作为纯粹的客观数据参考，避免干扰交易者的系统纪律。

## 六、后端API变更

### 6.1 `/chan/matrix/v2/{symbol}` 响应结构扩展 (V2 API 隔离)

> **依赖隔离说明**：为了不破坏现有的 `ChanView.jsx` 和 `SandTable.jsx` 页面，我们在 `chan.py` 中新增独占的 `v2` 路由供新雷达开发使用，旧接口原封不动保留。

在新的 V2 API 响应中，新增以下字段支持优雅降级和状态透传：

```python
{
  # 新增：顶层错误捕获 (应对上游挂掉或新股数据不足)
  "error": {
    "code": str,       # "API_RATE_LIMIT" | "INSUFFICIENT_DATA" | None
    "message": str,
    "fallback_used": bool
  },

  # 新增：入场条件逐一检测（空仓视角）
  "entry_checklist": {
    "day_buy_node": bool,       # 日线二买/类二买
    "m30_resonance": bool,      # 30分共振
    "m5_beichi": bool,          # 5分底背驰
    "window_valid": bool,       # 窗口有效
    "weekly_safe": bool,        # 周线无压制
    "score": int,               # 0-5，满足几个
    "missing": list[str],       # 未满足的条件列表
    "actionable": bool          # 五个全满足才为True
  },

  # 新增：止损ATR校验
  "stop_atr_check": {
    "stop_price": float,
    "stop_basis": str,          # "30分中枢ZG" / "30分一买低点" etc.
    "atr": float,
    "atr_multiple": float,      # 止损距离 / ATR
    "valid": bool,              # 1~2.5之间为True
    "note": str
  },

  # 新增：建议仓位
  "position_sizing": {
    "risk_pct": float,          # 默认1%
    "suggested_shares": int,
    "suggested_amount": float,
    "max_loss": float
  },

  # 新增：目标价列表
  "targets": [
    {"price": float, "label": str, "distance_pct": float}
  ],

  # 新增：持仓阶段（持仓模式专用，含Stage 0走势验证）
  "holding_stage": {
    "stage": int,               # 0-5，0为走势验证期
    "label": str,               # "走势验证期" / "验证期" etc.
    "trailing_stop": float,     # 台阶止损价
    "locked_profit_pct": float, # 即使触发台阶止损锁定的利润%
    "m30_top_beichi": str,      # "无" / "出现" / "确认"
    "day_top_beichi": str,      # "无" / "出现" / "确认"
    "action": str,              # "持有" / "减仓50%" / "清仓" / "建议出场（预案失效）"
    # Stage 0 专用字段
    "validation": {
      "m30_bi_direction": str,      # "向上" / "向下" / "未形成"
      "m30_bi_complete": bool,
      "bars_since_entry": int,
      "bars_remaining": int,        # 距验证截止还剩几根30分K线
      "status": str                 # "验证中" / "验证通过" / "预案失效" / "时间失效"
    }
  }
}
```

### 6.2 `/agent/radar_deduce` AI层约束

在 RADAR_SYSTEM_PROMPT 中增加约束：

```
你只能解读算法已经给出的结论，不能自行创造新的分析。
如果算法说"5分背驰未形成"，你只能说"当前5分信号尚未出现"，
不能用其他方式暗示可以入场。
context_json 中的 entry_checklist.actionable=false 时，
你的结论必须是"当前不建议入场"，不能给出任何模糊的积极暗示。
```

---

## 七、后端实现任务分解

### Phase 1：规则引擎（2-3天）

| 任务 | 文件 | 说明 |
|------|------|------|
| 重构背驰计算，区分段/笔/盘整三种类型 | `chan_service.py` | 新增 `_calc_beichi(bis, macd, type)` |
| 实现入场五条件逐一检测 | `chan_service.py` | 新增 `_build_entry_checklist()` |
| 修正三买定义（突破后回调不破ZG） | `chan_service.py` | 修改 `_detect_lifecycle_node` |
| 走势类型对买点置信度的影响 | `chan_service.py` | 修改 `_classify_zoushi` |
| 止损ATR合理性校验 | `chan_service.py` | 新增 `_check_stop_atr()` |
| 目标价计算 | `chan_service.py` | 新增 `_calc_target_price()` |
| 建议仓位计算 | `chan_service.py` | 新增 `_calc_position_size()` |

### Phase 2：持仓状态机（2-3天）

| 任务 | 文件 | 说明 |
|------|------|------|
| 实现Stage 0走势验证期 | `chan_service.py` | 新增 `_validate_entry_thesis()`，检测入场后30分笔方向 |
| 实现止盈六阶段状态机 | `chan_service.py` | 新增 `_detect_holding_stage()`，含Stage 0-5 |
| 台阶止损跟踪逻辑 | `chan_service.py` | 随新中枢形成自动更新，只上移不下移 |
| 中继背驰 vs 转折背驰区分 | `chan_service.py` | 顶背驰后等待价格走势确认 |
| 顶背驰检测（30分/日线） | `chan_service.py` | 对称实现卖点背驰 |
| 预案失效推送 | `price_monitor.py` | Stage 0失效时立即推送 |

### Phase 3：API & 推送（1天）

| 任务 | 文件 | 说明 |
|------|------|------|
| 扩展 `/chan/matrix/v2` 路由及响应 | `chan.py` | 新增独占路由加入所有新字段，并添加短效（如 15 秒）的结果级缓存，防止前端轮询压垮计算资源。**[运维要求]** 每次 API 被调用时，必须输出结构化日志（包含计算耗时 ms、缓存是否命中、最终的 holding_stage 状态）。若底层算法因数组越界等未知异常崩溃被 `try...except` 捕获降级，必须打印明确的 `[ERROR] CHAN_ENGINE_FALLBACK: {symbol}` 及堆栈，严禁静默失败。 |
| 更新 AI prompt 约束 | `radar_prompt.py` | 加入叙事约束规则 |
| 完善推送触发条件 | `price_monitor.py` | 止损/背驰/信号触发推送 |

### Phase 4：前端UI重构（2-3天）

| 任务 | 文件 | 说明 |
|------|------|------|
| 持仓状态自动检测 | `TRadarV2.jsx` | 先查持仓，再决定渲染模式 |
| 数据流轮询更新 | `TRadarV2.jsx` | 默认每 30 秒轮询一次 `/chan/matrix`。如果检测到持仓模式发生切换（空仓↔持仓），不强制重绘，而是弹出 Toast 提示：“持仓状态已更新，点击刷新雷达视图”。 |
| 结构状态面板（空仓版） | `TRadarV2.jsx` | 入场条件逐一展示，增加**风险设置小齿轮**，允许用户修改单笔风险比例（默认1%），该配置保存至后端数据库。 |
| 结构状态面板（持仓版） | `TRadarV2.jsx` | 台阶止损/锁定利润/背驰状态 |
| 甲乙丙卡片重构 | `TRadarV2.jsx` | 按空仓/持仓模式渲染不同内容 |
| AI解读折叠区 | `TRadarV2.jsx` | 按需加载，加上约束标注 |

### Phase 5：T+0日内交易模块（实时数据接入后）

> **前置条件：** 已接入实时行情数据（1分钟级别），Phase 1-4验收通过。
> T+0建立在主仓位逻辑完全稳定之后，不提前实施。

| 任务 | 文件 | 说明 |
|------|------|------|
| 接入1分钟K线数据 | `chan_service.py` | 扩展级别链：日线→30分→5分→1分 |
| 1分钟缠论结构计算 | `chan_service.py` | bis/zhongshus/beichi，复用现有引擎 |
| T+0入场条件检测 | `chan_service.py` | 新增 `_detect_t0_opportunity()`，五条约束全满足才激活 |
| T+0卖出触发 | `chan_service.py` | 1分钟一卖/类一卖信号识别 |
| T+0买回触发 | `chan_service.py` | 1分钟一买/二买/三买信号识别 |
| 14:30强制买回提醒 | `price_monitor.py` | T+0未闭环时，14:30推送强制买回通知 |
| T+0面板（前端） | `TRadarV2.jsx` | Stage 3+且5分顶背驰时激活，含倒计时 |
| T+0记录入库 | `trades.py` | T+0操作单独记录，关联原持仓 |

---

## 八、验收标准与测试契约 (Test Contract)

> **测试策略说明**：经评估，构建 Stage 0 失败的自动化单元测试成本过高。因此，放弃编写针对状态机流转的自动化测试，转而完全依赖于**沙盘推演 (Sand Table) 模块的肉眼人工观察**进行测试验证。

每个Phase完成后的验收条件：

**Phase 1验收：**
- 用真实股票测试，五个入场条件能逐一正确检测
- 止损ATR校验能正确拒绝止损过紧/过宽的情况
- 三买识别不在突破当天触发，必须等回调确认

**Phase 2验收：**
- 入场后立即进入Stage 0，30分走出上涨笔后自动切换到Stage 1
- 入场后30分走出向下笔，输出"预案失效，建议出场"并推送通知
- 超过10根30分K线未见上涨笔，输出"时间失效，建议出场"并推送通知
- 阶段随浮盈变化自动切换，Stage 0不因浮盈变化而跳过
- 台阶止损随价格上涨自动上移，不下移
- 30分顶背驰出现后，能正确区分中继和转折（等后续确认）

**Phase 3验收：**
- API响应包含所有新字段，无字段缺失
- AI推演在 `actionable=false` 时，叙事不包含入场建议
- 止损触发能在5分钟内推送通知

**Phase 4验收：**
- 有持仓时自动进入持仓模式，无需用户切换
- 五个条件中未满足的条件有明确标注
- 甲绿/乙黄/丙红状态颜色正确反映算法判断

---

**Phase 5验收：**
- T+0面板只在 Stage 3+ 且 5分顶背驰同时满足时激活，其余状态不显示
- 14:30前未闭环，推送强制买回通知，不得带到第二天
- T+0操作有独立记录，不影响主仓位持仓成本的展示逻辑
- 当天价格波动 < 1.5% 时，T+0面板显示"波动不足，不建议操作"

---

## 九、完整级别链总览

| 阶段 | 级别链 | 用途 |
|------|--------|------|
| Phase 1-4（当前） | 周线→日线→30分→5分 | 入场判断、持仓管理、利润保护 |
| Phase 5（T+0） | 周线→日线→30分→5分→1分 | 日内T+0执行（仅Stage 3+） |

各级别职责不变，1分钟只作为T+0的执行精度层，不参与主仓位的入场和出场判断。

---

*方案版本：v2.1*
*创建时间：2026-04-21*
*依赖文档：缠论交易框架与算法设计规范.md*
