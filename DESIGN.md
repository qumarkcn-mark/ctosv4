# Design System — CT-OS V4.0 交易教练

## 产品上下文 Product Context

- **产品定位:** 基于缠论 (Chan Theory) 的 A 股交易教练平台 — 记录交易、纠正行为、提醒机会
- **目标用户:** 使用缠论做技术分析的散户/半专业交易员，在券商 App 手动交易
- **竞品空间:** 雪球组合、韭菜公社、交易日志类工具、TradingView Alert
- **项目类型:** 桌面 Web App (全功能) + 微信小程序 (轻量交互)

## 美学方向 Aesthetic Direction

- **方向:** 工业/军用功能主义 (Industrial-Military Utilitarian)
- **装饰级别:** 极简（minimal）— 字体和间距完成所有层次传达，不依赖装饰性元素
- **设计语气:** 潜艇作战室控制台、无人机操作面板、SpaceX Mission Control
- **唯一允许的视觉装饰:** 功能性的状态灯呼吸、雷达扫描线、信号强度示意
- **参考方向:** 精密、克制、值得信任的作战系统。每个像素必须承载信息或辅助决策

## 字体系统 Typography

### 字体族

| 角色 | 字体 | 备选 | 理由 |
|------|------|------|------|
| **显示/标题** | Geist | Outfit | Vercel 出品，几何感强，科技感十足，比 Inter 更锐利、更有性格 |
| **正文/UI** | DM Sans | Source Sans 3 | x-height 大易读性好，原生支持 `tabular-nums`，数据场景完美 |
| **数据/表格** | JetBrains Mono | Geist Mono | 等宽字体标杆，适合价格、代码、计量数据 |
| **代码** | JetBrains Mono | Fira Code | 保持与数据字体一致性 |
| **中文** | HarmonyOS Sans SC | Noto Sans SC (思源黑体) | 鸿蒙字体笔画经过优化，小字号清晰度极好 |

### 加载方式

```html
<!-- Latin 字体 via Google Fonts -->
<link href="https://fonts.googleapis.com/css2?family=Geist:wght@400;500;600;700;800;900&family=DM+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600;700&display=swap" rel="stylesheet">

<!-- 中文字体 via CDN -->
<!-- HarmonyOS Sans SC 需从华为 CDN 或自托管加载 -->
<!-- 备选: Google Fonts 的 Noto Sans SC -->
```

### CSS 变量

```css
--font-display: 'Geist', -apple-system, BlinkMacSystemFont, sans-serif;
--font-body: 'DM Sans', 'HarmonyOS Sans SC', 'Noto Sans SC', sans-serif;
--font-mono: 'JetBrains Mono', 'SF Mono', 'Fira Code', monospace;
--font-cn: 'HarmonyOS Sans SC', 'Noto Sans SC', 'PingFang SC', sans-serif;
```

### 字号阶梯

```css
--text-2xs: 10px;   /* 辅助标注、时间戳 */
--text-xs:  11px;   /* 状态标签、元数据 */
--text-sm:  12px;   /* 表格数据、次要信息 */
--text-base: 13px;  /* 正文、UI 标签（基准，非 16px） */
--text-md:  14px;   /* 面板标题 */
--text-lg:  16px;   /* 区域标题 */
--text-xl:  20px;   /* 指挥沙盘标题 */
--text-2xl: 28px;   /* 核心数字（当前价格） */
--text-3xl: 36px;   /* 英雄数字（账户总值） */
```

> **注意：** 基准 13px 是交易终端标准。信息密度高，专业用户习惯小字号。

### 字重使用规则

| 字重 | 用途 |
|------|------|
| 400 Regular | 正文、描述文本 |
| 500 Medium | UI 标签、表头 |
| 600 Semibold | 面板标题、激活状态 |
| 700 Bold | 区域标题、核心数据 |
| 800 Extrabold | 英雄数字、指挥沙盘标题 |

## 色彩系统 Color

### 策略

**收敛型 (restrained)** — 色彩是稀缺的、有意义的。90% 的界面是灰阶，颜色只在需要传达状态时出现。

### CSS 变量

```css
:root {
  /* ── 底面层级（5级深度系统）── */
  --surface-0: #08080a;        /* 最深底面：全屏背景 */
  --surface-1: #0d0d0f;        /* 主面板背景 */
  --surface-2: #111115;        /* 卡片/浮层 */
  --surface-3: #18181e;        /* hover 状态 / 抬升面板 */
  --surface-4: #1e1e26;        /* 活跃选中态 */

  /* ── 边框 ── */
  --border-subtle: rgba(255, 255, 255, 0.06);
  --border-default: rgba(255, 255, 255, 0.10);
  --border-strong: rgba(255, 255, 255, 0.18);

  /* ── 文字 ── */
  --text-primary: rgba(255, 255, 255, 0.92);
  --text-secondary: rgba(255, 255, 255, 0.55);
  --text-tertiary: rgba(255, 255, 255, 0.32);
  --text-disabled: rgba(255, 255, 255, 0.18);

  /* ── A股标准色（红涨绿跌，法定标准）── */
  --bull-red: #f43f5e;
  --bull-red-muted: rgba(244, 63, 94, 0.15);
  --bull-red-glow: rgba(244, 63, 94, 0.40);
  --bear-green: #10b981;
  --bear-green-muted: rgba(16, 185, 129, 0.15);

  /* ── 功能色 ── */
  --accent-cyan: #06b6d4;       /* 信息/扫描/雷达 */
  --accent-cyan-muted: rgba(6, 182, 212, 0.15);
  --signal-amber: #f59e0b;      /* 警告/待确认 */
  --signal-amber-muted: rgba(245, 158, 11, 0.15);

  /* ── 交易教练功能色（V4.0）── */
  --action-gold: #c8a832;       /* 建议行动金：强调/建议关注 */
  --action-gold-glow: rgba(200, 168, 50, 0.35);
  --action-gold-muted: rgba(200, 168, 50, 0.12);
  --action-muted: #6b7280;      /* 冷灰：次要操作 */
  --warning-red: #dc2626;       /* 风控红：止损/逆势警告 */
  --cmd-veto-glow: rgba(220, 38, 38, 0.40);

  /* ── Agent 状态灯 ── */
  --agent-idle: rgba(255, 255, 255, 0.12);      /* 待机灰 */
  --agent-scanning: var(--accent-cyan);         /* 扫描中 */
  --agent-clear: #22c55e;                       /* 通过绿 */
  --agent-veto: var(--cmd-veto);                /* 否决红 */
  --agent-timeout: var(--signal-amber);         /* 超时橙 */
}
```

### 色彩设计意图

1. **`--action-gold` 金色** — 用于系统建议关注的内容（信号提醒、建议止损价、回补窗口）
2. **`--action-muted` 冷灰** — 次要操作、取消、关闭
3. **`--warning-red` 红色** — 风控警告：止损穿越、逆势建仓、仓位超限。注意与 A 股红涨色区分，此色仅用于警告图标和边框
4. **5 级底面层级** — 每级差 4-6 亮度值，面板/浮层/卡片的空间深度
5. **muted 变体** — 每个强调色都有 12-15% 不透明度的低调版本，用于背景色、徽章底色

## 间距系统 Spacing

### 基准单元

**4px** — 交易终端的信息密度要求紧凑间距

### 密度

**紧凑 (compact)** — 专业工具，不是消费品应用

### 间距阶梯

```css
--space-0:  0px;
--space-1:  2px;    /* 组件内微间距 */
--space-2:  4px;    /* 紧密元素间 */
--space-3:  6px;    /* 相关元素组内 */
--space-4:  8px;    /* 标准内间距 */
--space-5:  12px;   /* 小分组间距 */
--space-6:  16px;   /* 区块间距 */
--space-7:  24px;   /* 面板间距 */
--space-8:  32px;   /* 大区域分隔 */
--space-9:  48px;   /* 版块级分隔 */
```

## 布局系统 Layout

### 策略

**网格严格型 (grid-disciplined)** — 面板固定位置，工作区流式

### 主布局（桌面端 ≥1440px，主目标）

```text
┌──────────────────────────────────────────────────┐
│ 顶部状态栏 (系统时间/连接状态/账户概览)  h:48px  │
├─────────┬───────────────────────┬────────────────┤
│ 自选股  │    主工作区           │  指令面板      │
│ 侧栏    │   (图表/指挥沙盘)    │ (雷达/Agent)   │
│ w:280px │    flex-1             │  w:360px       │
│         │                       │                │
├─────────┴───────────────────────┴────────────────┤
│ 底部状态条 (日志/最近交易/系统健康)  h:28px      │
└──────────────────────────────────────────────────┘
```

### 视图层级

| 层级 | 描述 | 导航 |
|------|------|------|
| **L0 交易看板** | 持仓概览 + 仓位透视 + 信号提醒队列 | 主视图，默认显示 |
| **L1 缠论看盘** | K 线图 + 缠论叠加 + 推演沙盘 | 点击持仓/信号卡进入 |
| **L2 行为分析** | 交易日志 + 胜率/纪律/模式分析 | 菜单切换 |
| **L3 交易录入** | 语音/手动交易录入 + CSV 导入 | 浮层/快捷键 |

### 圆角阶梯

```css
--radius-xs: 2px;      /* 按钮内图标 */
--radius-sm: 4px;      /* 输入框、小按钮 */
--radius-md: 6px;      /* 卡片、面板 */
--radius-lg: 8px;      /* 弹窗、浮层 */
--radius-xl: 12px;     /* 信号卡、大弹窗 */
--radius-full: 9999px; /* 状态徽章、指示灯 */
```

> **注意：** 圆角故意偏小。交易终端追求精密感，大圆角显得漫不经心。

### 响应式断点

| 断点 | 宽度 | 布局变化 |
|------|------|----------|
| Desktop XL | ≥1920px | 加宽所有面板 |
| Desktop | ≥1440px | **主目标布局** |
| Desktop SM | ≥1280px | 收窄侧栏至 240px |
| Tablet | ≥1024px | 侧栏可折叠 |
| Mobile | <1024px | 不做主支持，提示"请在桌面端使用" |

## 动效系统 Motion

### 策略

**有意图的 (intentional)** — 每个动效必须传达系统状态信息，禁止纯装饰动画

### 时间曲线

```css
--ease-enter: cubic-bezier(0.16, 1, 0.3, 1);     /* 进入：快速减速 */
--ease-exit: cubic-bezier(0.4, 0, 1, 1);          /* 退出：加速消失 */
--ease-move: cubic-bezier(0.4, 0, 0.2, 1);        /* 位移：标准 */
--ease-bounce: cubic-bezier(0.34, 1.56, 0.64, 1); /* 弹性：强调 */
```

### 时长

```css
--duration-instant: 50ms;     /* 状态灯切换 */
--duration-micro: 100ms;      /* hover 反馈 */
--duration-fast: 150ms;       /* 小组件过渡 */
--duration-normal: 250ms;     /* 面板切换 */
--duration-slow: 400ms;       /* 大区域过渡 */
--duration-emphasis: 600ms;   /* 信号卡弹出 */
```

### 动效清单

| 动效名 | 用途 | 来源 |
|--------|------|------|
| `slideInRight` | 信号提醒从右侧入场 | ✅ 已有 |
| `breathingPulse` | 止损预警呼吸灯 | ✅ 已有 |
| `scorePop` | 分值数字弹出 | ✅ 已有 |
| `liveBeacon` | 实时灯塔闪烁 | ✅ 已有 |
| `scanSweep` | 空状态扫描线 | ✅ 已有 |
| `warningFlash` | 风控警告时卡片红色闪烁 1 次 | 🆕 V4.0 |
| `agentProgress` | Agent 扫描进度条，青色从左到右填充 | 🆕 V4.0 |

### 动效规则

1. **动效 = 信息**：如果一个动画去掉后用户不会丢失任何信息，那它就不应该存在
2. **减少运动偏好**：遵守 `prefers-reduced-motion`，在减少运动模式下所有动画变为即时切换
3. **状态转换优先**：告警状态从 normal → warning 的过渡必须有动画，否则用户可能错过变化

## 组件规格 Component Specs

### 信号提醒卡 (AlertCard)

信号提醒卡用于各类提醒通知，不含交易执行按钮。

```text
┌─ AlertCard ───────────────────────────────────────┐
│  🔔 止损预警                       14:23:05       │
│  ────────────────────────────────────────────────  │
│  600519 贵州茅台  ¥1,752                          │
│  距止损 ¥1,746 仅剩 0.3%                          │
│  ────────────────────────────────────────────────  │
│  ATR 建议: 若穿 ¥1,746 建议卖出                    │
│  历史数据: 未止损平均多亏 4.3%                      │
│  ┌──────────┐  ┌──────────┐                       │
│  │ 忽略      │  │ 设提醒    │                       │
│  └──────────┘  └──────────┘                       │
└───────────────────────────────────────────────────┘
```

- 背景: `--surface-2`
- 边框: `--border-default`, 风控警告时 `--warning-red` 左侧 3px 边框
- 信号类提醒: `--action-gold` 左侧 3px 边框
- [设提醒] 按钮: `--action-gold` 底色
- [忽略] 按钮: `--action-muted` 底色

### 风控警告卡行为

- 止损穿越 / 逆势建仓 / 仓位超限: `warningFlash` 红色闪烁 1 次
- 警告理由: 卡片底部显示历史数据 (如 "未止损 N 次平均多亏 X%")
- 保留直到用户操作 (忽略/设提醒)

### 交易录入表 (TradeEntryForm)

```text
┌─ 录入交易 ────────────────────────────────────────┐
│                                                    │
│  [🎤 语音录入]  "买了 300 股茅台 1800"              │
│  ────────────────────────────────────────────────  │
│  方向:    ◉ 买入  ○ 卖出                           │
│  标的:    [600519 贵州茅台    ▾]                    │
│  价格:    [¥1,800.00        ]                      │
│  数量:    [300              ] 股                    │
│  ────────────────────────────────────────────────  │
│  ⚠️ 级别检查:                                      │
│  30分 ↗ 上涨  日线 ↗ 上涨  周线 →  盘整            │
│  ────────────────────────────────────────────────  │
│  ATR 建议止损: ¥1,746  (距当前 -3%)                 │
│  ────────────────────────────────────────────────  │
│  交易原因: [LLM 自动提取 / 手动选择]               │
│  ┌──────────┐  ┌──────────────────────┐            │
│  │ 取消      │  │ 确认录入              │            │
│  └──────────┘  └──────────────────────┘            │
└────────────────────────────────────────────────────┘
```

- 背景: `--surface-2`
- 语音按钮: `--accent-cyan` 圆形，录音时 `breathingPulse`
- 级别检查区: 自动填充，↗ 用 `--bull-red`，↘ 用 `--bear-green`，→ 用 `--text-tertiary`
- 逆势警告: 若方向与日线/周线矛盾，显示 `--warning-red` 警告框
- [确认录入] 按钮: `--action-gold`
- 弹出方式: 浮层(overlay) 或快捷键 `Cmd+N`

### 仓位透视图 (PositionOverview)

```text
┌─ 仓位透视 ────────────────────────────────────────┐
│                                                    │
│   ┌─── 饼图 ───┐    持仓列表 (按金额排序)          │
│   │   茅台 18%  │    1. 600519 茅台  ¥180,000 18%  │
│   │   宁德 15%  │    2. 300750 宁德  ¥150,000 15%  │
│   │   比亚迪12% │    3. 002594 迪迪  ¥120,000 12%  │
│   │   小票群32% │    ────────── 以下小票 ──────── │
│   │   现金 23%  │    4. 000001 ...    ¥50,000  5%  │
│   └─────────────┘    ...                           │
│                                                    │
│   ⚠️ 预警: 7 只小票合计 ¥320,000 (32%)            │
│   ⚠️ 预警: 持仓 10 只 (建议 ≤5)                   │
└────────────────────────────────────────────────────┘
```

- 饼图: 使用 A 股标准色标，小票群用 `--text-tertiary` 灰色
- 预警文字: `--signal-amber` 背景 `--signal-amber-muted`
- 超限预警: `--warning-red` 背景
- 金额: `--font-mono` + `tabular-nums`

### 行为分析图表 (BehaviorDashboard)

```text
┌─ 交易行为分析 (本月) ─────────────────────────────┐
│                                                    │
│  胜率: 58%    盈亏比: 1.8:1    最大回撤: -4.2%     │
│                                                    │
│  ┌── 纪律仪表盘 ──────────────────────────────┐    │
│  │                                              │   │
│  │  止损执行率: ██████████░░ 72%  (目标100%)   │   │
│  │  顺势交易率: ████████░░░░ 65%  (目标80%)    │   │
│  │  提前卖出:   3 次 (占盈利交易 25%)           │   │
│  │  逆势建仓:   2 次 (胜率 0%)                  │   │
│  │                                              │   │
│  └──────────────────────────────────────────────┘   │
│                                                    │
│  你的强项: 缠论一买入场 (胜率 75%)                  │
│  你的弱项: 追涨三买后 (胜率 33%，建议停止)          │
└────────────────────────────────────────────────────┘
```

- 进度条: 达标用 `--agent-clear` 绿色，未达标用 `--signal-amber`
- 核心数字: `--text-2xl` + `--font-mono`
- 强项/弱项: 强项用 `--agent-clear`，弱项用 `--warning-red`

### 顶部状态栏 (StatusBar)

- 高度: 48px
- 背景: `--surface-1` + `backdrop-filter: blur(16px)`
- 左侧: 交易教练 logo + 连接状态灯
- 中部: 总资产 / 今日盈亏 (`--font-mono` 等宽 + `tabular-nums`)
- 右侧: [🎤 快速录入] 按钮 + 通知铃铛 + 头像
- 总资产: `--text-primary`, 盈亏用 `--bull-red` / `--bear-green`

### Exit Guardian 追踪止损可视化

两种并行展示方式:

1. K 线图叠加 ATR 追踪线 (分屏模式左侧)
   - ATR 追踪线: `--cmd-authorize` 金色虚线
   - 当前止损价标注: 右侧 Y 轴标签
   - 价格穿过追踪线时: 线变 `--cmd-veto` 红色
2. 独立数字面板 (右侧指令面板内)
   - 当前价: `--text-2xl` 大字
   - ATR 值: `--font-mono`
   - 追踪止损价: `--cmd-authorize` 金色
   - 距离止损 %: `--font-mono`, 接近时变 `--signal-amber`

## 无障碍 Accessibility

- 键盘导航：所有交互元素可 Tab 聚焦，Enter/Space 激活
- 对比度：文字/背景对比度 ≥ 4.5:1（WCAG AA）
- 触控目标：所有按钮最小 32x32px（桌面端），移动端不做主支持
- ARIA：状态灯使用 `role="status"` + `aria-label` 描述状态
- 减少运动：遵守 `prefers-reduced-motion` 媒体查询

## 迁移说明（从 V3 到 V4）

### 需要修改的 CSS 变量映射

| V3 变量 | V4 变量 | 说明 |
|---------|---------|------|
| `--radar-bg` | `--surface-1` | 统一到 5 级底面系统 |
| `--radar-surface` | `--surface-2` | 同上 |
| `--radar-card` | `--surface-2` | 卡片底色 |
| `--radar-card-hover` | `--surface-3` | hover 态 |
| `--radar-border` | `--border-subtle` | 统一命名 |
| `--radar-border-hover` | `--border-default` | 同上 |
| `--alert-red` | `--bull-red` | 语义更清晰 |
| `--buy-red` | `--bull-red` | 合并重复 |
| `--sell-green` | `--bear-green` | 语义更清晰 |
| `--font-sans` | `--font-display` / `--font-body` | 分离显示和正文字体 |
| `--font-mono` | `--font-mono` | 保持不变 |

## 交互规格 Interaction Specs

### 导航架构

| 视图 | 说明 | 进入方式 |
|------|------|----------|
| L0 交易看板 | 持仓概览 + 仓位透视 + 提醒队列 | 默认主视图 |
| L0 分屏模式 | 左 60% K 线图 + 右 40% 持仓详情 | 在 L0 点击持仓/信号 |
| L1 缠论看盘 | 完整缠论分析 + 推演沙盘 | 分屏内 完整分析 按钮 |
| L2 行为分析 | 交易日志 + 行为报告 | 底部 Tab 切换 |
| L3 交易录入 | 语音/手动/CSV导入 | Cmd+N 或浮层 |

### 核心交互流

**录入交易:**

1. Cmd+N 或点击 [🎤 快速录入] → 浮层弹出交易录入表
2. 语音说 "买了 300 股茅台 1800" → LLM 提取 → 自动填充表单
3. 系统自动: 检查多级别方向 + 计算 ATR 止损 + 仓位占比
4. 逆势建仓? → 显示历史数据警告
5. 用户确认 → 保存 → 仓位透视立即更新

**查看持仓:**

- 点击持仓 → 分屏: 左 60% K 线图(含 ATR 止损线) + 右 40% 持仓信息
- 持仓信息: 成本价/当前价/盈亏/缠论趋势状态/ATR 止损价
- 回本时 → 回本陷阱警报滑入

**接收提醒:**

- 信号/止损提醒 → slideInRight 卡片入场
- 用户操作: [忽略] / [设提醒] / 点击展开详情
- 风控警告 → warningFlash + 历史数据

### 空状态设计

无持仓且无提醒时:

- 中心: 引导 "录入第一笔交易" 或 "导入交割单"
- 底部: 自选股池 XX 只标的 | 系统扫描中

## 微信小程序设计适配

### 原则

- 只做查看和录入，不做复杂分析图表
- 色彩系统与桌面端保持一致
- 字号阶梯上调 2px (基准 15px，适配移动端)
- 圆角上调 (小程序标准 8-16px)

### 页面结构

| 页面 | 功能 |
|------|------|
| 首页 | 持仓概览 + 今日盈亏 + 提醒列表 |
| 录入 | 语音/手动交易录入 |
| 持仓 | 持仓列表 + 仓位饼图 (简化版) |
| 分析 | 推演沙盘结果 (静态图片) + 分享 |
| 我的 | 设置 + 登录状态 |

### 推送消息模板

| 类型 | 标题 | 内容 |
|------|------|------|
| 止损预警 | ⚠️ 止损预警 | {股票名} 距止损 {止损价} 仅剩 {百分比} |
| 紧急止损 | 🚨 止损穿越 | {股票名} 已穿止损线! 建议立即处理 |
| 信号提醒 | 📡 缠论信号 | {股票名} 出现 {信号类型}，评分 {分数}/100 |
| 回补窗口 | 🔄 回补机会 | {股票名} 已到回补区间 {价格区间} |
| 盘后发现 | 🔍 盘后扫描 | 发现 {数量} 只新候选标的 |

## 决策日志 Decisions Log

| 日期 | 决策 | 理由 |
|------|------|------|
| 2026-04-09 | 初始设计系统创建 | 由 /design-consultation 基于现有 CSS + V4.0 定位创建 |
| 2026-04-09 | Geist 替换 Inter 作为显示字体 | Inter 过度使用, Geist 更锐利 |
| 2026-04-09 | 金色用于强调/建议行动 | A 股红涨绿跌, 绿色不可用于正面操作 |
| 2026-04-09 | 基准字号 13px (桌面) / 15px (小程序) | 交易终端信息密度标准 |
| 2026-04-09 | 圆角偏小 (桌面 2-12px / 小程序 8-16px) | 桌面精密感, 小程序平台标准 |
| 2026-04-10 | 产品从"交易机器人"转为"交易教练" | 用户需求: 记录/纠正/提醒, 非自动执行 |
| 2026-04-10 | 删除授权发射按钮和3秒冷静期 | 用户在券商App交易, 系统不执行 |
| 2026-04-10 | 新增交易录入表 + 语音输入 | 核心交互: 快速记录真实交易 |
| 2026-04-10 | 新增仓位透视图 + 行为分析 | 六把武器的视觉载体 |
| 2026-04-10 | 新增微信小程序设计适配 | 移动端查看/录入/接收推送 |
| 2026-04-10 | ATR追踪止损保留双通道可视化 | K线叠加(直觉) + 面板数字(精确) |
